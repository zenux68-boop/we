minimizeBtn.Activated:Connect(function()
    setMinimized(true)
end)

restoreBtn.BackgroundTransparency = 0.99  -- forces touch hitbox to register
restoreBtn.Activated:Connect(function()
    setMinimized(false)
end)
